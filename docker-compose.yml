services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: uzinex
      POSTGRES_USER: uzinex
      POSTGRES_PASSWORD: uzinexpass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

